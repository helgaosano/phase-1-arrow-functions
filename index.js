/*const divide = (num1, num2) => { 
    let result = num1 / num2;
    return result;
}
divide(2000, 100);

const square = (10) => 
*/
const divide = (num1, num2) => {
    return 2000 / 100;
}
divide()

const square = (x) => x * x;
square(2);

const add = (a, b) => a + b;
add(3, 4)